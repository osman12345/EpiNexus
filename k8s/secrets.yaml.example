# EpiNexus Kubernetes Secrets Template
# =====================================
# Copy this file to secrets.yaml and replace placeholder values with real credentials.
#
#   cp secrets.yaml.example secrets.yaml
#   # Edit secrets.yaml with real passwords
#   kubectl apply -f secrets.yaml
#
# NEVER commit secrets.yaml to version control.
# For production, consider using a secrets manager:
#   - HashiCorp Vault (https://www.vaultproject.io/)
#   - AWS Secrets Manager / Parameter Store
#   - Azure Key Vault
#   - Sealed Secrets (https://github.com/bitnami-labs/sealed-secrets)

apiVersion: v1
kind: Secret
metadata:
  name: epinexus-secrets
  namespace: epinexus
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_WITH_STRONG_PASSWORD"
  REDIS_PASSWORD: "REPLACE_WITH_STRONG_PASSWORD"
